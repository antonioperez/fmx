import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    logo: {
    },
    appText: {
      color: "white",
    },
    clearButton: {
      backgroundColor: 'transparent', 
      borderRadius: 10,  
      borderColor: "#fff",
      borderWidth: 1
    }
  });

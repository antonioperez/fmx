import {
    StyleSheet
} from 'react-native';

export const welcomeStyle = StyleSheet.create({
    centerSlider: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    },
    wrapper: {

    },
});